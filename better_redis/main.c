/* main.c */
#include "lib.h"

bool scontiunation;

void mainloop() {
  struct sockaddr_in sock;
  int s;
}

int main(int argc, char *argv[]) {
  scontiunation = true;
  while(scontiunation)
      mainloop();  
  
  
  
  return 0;
}
